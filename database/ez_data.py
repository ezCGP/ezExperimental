"""Contains abstract class of ezCGP data objects. All Data that is passed through the block structure should inherit this.
The exact type of data you want depends on the problem context.
The type of data that is passed through can evolve from block-to-block but they should all inherit from this abstract class"""

class ezData():
    pass